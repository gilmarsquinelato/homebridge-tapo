{
  "pluginAlias": "TapoSmartHome",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for TP-Link Tapo smart devices. Devices are discovered automatically.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Tapo Smart Home",
        "required": true
      },
      "email": {
        "title": "TP-Link Account Email",
        "type": "string",
        "required": true,
        "description": "The email address used for your TP-Link/Tapo account"
      },
      "password": {
        "title": "TP-Link Account Password",
        "type": "string",
        "required": true,
        "description": "The password for your TP-Link/Tapo account"
      },
      "broadcastAddress": {
        "title": "Broadcast Address",
        "type": "string",
        "default": "255.255.255.255",
        "description": "Network broadcast address for device discovery (e.g., 192.168.1.255)"
      },
      "discoveryTimeout": {
        "title": "Discovery Timeout (seconds)",
        "type": "integer",
        "default": 5,
        "minimum": 2,
        "maximum": 30,
        "description": "How long to wait for devices to respond during discovery"
      },
      "pollingInterval": {
        "title": "Discovery Interval (seconds)",
        "type": "integer",
        "default": 300,
        "minimum": 60,
        "description": "How often to re-run device discovery to pick up new or relocated devices"
      },
      "stateInterval": {
        "title": "State Polling Interval (seconds)",
        "type": "integer",
        "default": 10,
        "minimum": 3,
        "description": "How often to read each device's state so HomeKit reflects changes made outside of it (Tapo app, wall switch). Lower values react faster but generate more network traffic."
      }
    }
  }
}
